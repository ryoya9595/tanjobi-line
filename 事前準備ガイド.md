# 事前準備ガイド（誕生日LINE通知）

3つのサービスを使います。**所要 1時間〜1時間半**。

| サービス | 役割 | 費用 |
|---|---|---|
| GitHub | 顧客台帳を置く／毎朝の通知を動かす | 無料 |
| LINE | 登録の入口と、通知の届き先 | 無料 |
| Vercel | LINEからのメッセージを受け取る場所 | 無料 |

Claude Code と一緒に進める場合、画面の操作はClaude Codeが進めます。
あなたにお願いするのは **ログイン** と **トークンの貼り付け** だけです。

---

## ⚠️ 最初に必ず読んでください

**いま配信に使っているLINE公式アカウント（エルメ・Lステップにつないでいるもの）は使いません。**

1. そのアカウントで「チャネルアクセストークン」を発行し直すと、**エルメが持っている今のトークンが無効になり、配信が止まることがあります**
2. **Webhook URL は1アカウントに1つ**しか設定できません。上書きすると、エルメ側の受信が壊れます

そこで、通知専用のアカウントを**新しく無料で作ります**。友だちは**あなた1人だけ**です。

作業中、**「再発行」と書かれたボタンは押さないでください**。押すのは「発行」だけです。

---

## STEP 1｜GitHub の準備（20分）

### 1-1. 非公開リポジトリを作る

1. https://github.com/new を開く
2. 入力する：

| 項目 | 値 |
|---|---|
| Repository name | `tanjobi-line`（好きな名前でOK） |
| 公開設定 | 🔴 **Private** を選ぶ（**必ず**） |
| Add a README file | チェックしない |

3. 「Create repository」

> 🔴 **必ず Private にしてください。** このリポジトリにはお客様の氏名と生年月日が入ります。
> Public にすると、誰でも読める状態になります。

### 1-2. キットを置く

`kit/` の中身（`scripts/` `data/` `requirements.txt` `.github/`）を、このリポジトリの**直下**に置きます。
Claude Code が配置とpushを行います。

置いたあとの形：

```
tanjobi-line/
├── .github/workflows/birthday.yml
├── scripts/birthday_check.py
├── scripts/customers.py
├── scripts/notify.py
├── data/customers.csv      ← 台帳（最初はヘッダーだけ）
└── requirements.txt
```

### 1-3. アクセストークン（PAT）を作る

Vercel から台帳を読み書きするための鍵です。

1. https://github.com/settings/personal-access-tokens/new を開く
   （Settings → Developer settings → Personal access tokens → **Fine-grained tokens**）
2. 入力する：

| 項目 | 値 |
|---|---|
| Token name | `tanjobi-line` |
| Expiration | **1年** など（⚠️ 切れると登録できなくなります。カレンダーに更新リマインドを） |
| Repository access | **Only select repositories** → STEP 1-1 で作ったリポジトリ |

3. 「Repository permissions」で2つだけ許可する：

| 権限 | 設定 |
|---|---|
| **Contents** | Read and write |
| **Actions** | Read and write |

4. 「Generate token」→ 表示された文字列を**コピーして一時保存**
   - `github_pat_` で始まる長い文字列です
   - **この画面を離れると二度と表示されません**

---

## STEP 2｜通知用のLINEを作る（30分）

### 2-1. LINE公式アカウントを作る

1. https://manager.line.biz/ を開く
2. **LINEアカウントでログイン**（ふだんスマホで使っているLINEでOK）
3. 「アカウント作成」→「LINE公式アカウントを作成」
4. 入力する：

| 項目 | 入れるもの |
|---|---|
| アカウント名 | `誕生日メモ`（あとで変えられます。お客様には見えません） |
| メールアドレス | ふだん使っているもの |
| 業種 | 何でも構いません |

### 2-2. 応答設定を変える（重要）

自動応答が動いていると、コマンドへの返事と二重になります。

1. 左メニュー「**設定**」→「**応答設定**」
2. **応答モード**：`Bot`
3. **あいさつメッセージ**：オフ
4. **応答メッセージ**：🔴 **オフ**（ここを切らないと、登録のたびに定型文も返ってきます）
5. **Webhook**：🔴 **オン**

### 2-3. チャネルアクセストークンとチャネルシークレット

1. 左メニュー「**設定**」→「**Messaging API**」→「**Messaging APIを利用する**」
2. **プロバイダー**は**新しく作る**のがおすすめ（名前は屋号やお名前で）
3. プライバシーポリシー・利用規約のURLは空欄でOK →「同意する」→「OK」
4. https://developers.line.biz/console/ を開く
5. いま作ったプロバイダー → チャネル（`誕生日メモ`）を開く
6. 「**Messaging API設定**」タブ → 一番下「**チャネルアクセストークン（長期）**」の「**発行**」
   - 🔴 表示が「**再発行**」なら押さない。別のアカウントを開いています
   - 出てきた文字列をコピーして一時保存（**これがトークン**）
7. 「**チャネル基本設定**」タブ → 「**チャネルシークレット**」をコピーして一時保存
   - トークンとは別物です。両方必要です

### 2-4. あなたのユーザーID

1. 同じ「**チャネル基本設定**」タブの一番下 →「**あなたのユーザーID**」
2. `U` から始まる33文字ほどの文字列をコピー

> ⚠️ **必ず、いま作ったチャネルの画面から取ってください。** 別チャネルのIDだと通知が届きません。
> ⚠️ 「チャネルID」「LINE ID」とは別物です。

### 2-5. 自分で友だち追加する（忘れやすい）

1. 「**Messaging API設定**」タブの「**ボットのQRコード**」を表示
2. **スマホのLINE**で読み取って友だち追加

**ここを飛ばすと通知が届きません。** トーク一覧に `誕生日メモ` が出ていれば成功です。

---

## STEP 3｜GitHub に鍵を登録する（5分）

1. リポジトリの「Settings」→「Secrets and variables」→「**Actions**」
2. 「New repository secret」で2つ登録：

| Name | Value |
|---|---|
| `LINE_CHANNEL_ACCESS_TOKEN` | STEP 2-3 のトークン |
| `LINE_TO_USER_ID` | STEP 2-4 のユーザーID |

3. （任意）事前にも知らせたい場合は「**Variables**」タブで：

| Name | Value |
|---|---|
| `NOTIFY_DAYS_BEFORE` | `0` なら当日のみ（既定）。`0,3` なら当日と3日前 |

---

## STEP 4｜Vercel にデプロイする（20分）

1. https://vercel.com/signup で **GitHubアカウントでログイン**
2. 「Add New...」→「Project」→ STEP 1 のリポジトリを **Import**
3. 設定：

| 項目 | 値 |
|---|---|
| Framework Preset | **Other** |
| Root Directory | `webhook` ← 🔴 **これを忘れない** |

4. 「Environment Variables」に次を登録（値の貼り付けは本人が）：

| Name | Value |
|---|---|
| `LINE_CHANNEL_SECRET` | STEP 2-3 のチャネルシークレット |
| `LINE_CHANNEL_ACCESS_TOKEN` | STEP 2-3 のトークン |
| `ALLOWED_USER_ID` | STEP 2-4 のユーザーID |
| `GITHUB_TOKEN` | STEP 1-3 のPAT |
| `GITHUB_REPO` | `あなたのユーザー名/tanjobi-line` |

5. 「Deploy」→ 完了したらURLが出ます（`https://〇〇.vercel.app`）

---

## STEP 5｜LINEとVercelをつなぐ（5分）

1. https://developers.line.biz/console/ →（チャネル）→「Messaging API設定」
2. 「**Webhook URL**」に入力：

```
https://〇〇.vercel.app/api/webhook
```

（`〇〇` は STEP 4-5 のURL。**末尾の `/api/webhook` を忘れずに**）

3. 「更新」→「**検証**」を押して `成功` が出ることを確認
4. 「**Webhookの利用**」を**オン**

---

## STEP 6｜動作確認（10分）

スマホのLINEで `誕生日メモ` を開いて、順に送ります。

| 送る | 期待する返事 |
|---|---|
| `使い方` | コマンド一覧が返る |
| `テスト太郎 1990/06/23 動作確認` | 「✅ 登録しました」 |
| `一覧` | テスト太郎が出る |
| `今日` | 「確認しています」→ 該当なしなら何も来ない（正常） |

**通知そのものを試すには**、今日の日付で誰か1人を登録して `今日` と送ります。

```
テスト太郎 1985/（今日の月）/（今日の日） 通知テスト
```

1分ほどで「🎂 今日が誕生日（1名）」が届けば完成です。
確認できたら `削除 テスト太郎` で消してください。

---

## 控えメモ

| 名前 | どこで使った |
|---|---|
| GitHubのPAT | Vercel の `GITHUB_TOKEN` |
| チャネルアクセストークン | GitHub Secrets と Vercel の両方 |
| チャネルシークレット | Vercel の `LINE_CHANNEL_SECRET` |
| あなたのユーザーID | GitHub Secrets と Vercel の両方 |

---

## よくあるつまずき

| 症状 | 原因と対処 |
|---|---|
| LINEに送っても何も返ってこない | ① `ALLOWED_USER_ID` が違う（本人以外は黙って無視する設計）② Webhookの利用がオフ ③ Vercel の Root Directory が `webhook` になっていない |
| 定型文が一緒に返ってくる | LINE側の「応答メッセージ」がオン。STEP 2-2 でオフにする |
| 「GitHubに書き込めませんでした（403）」 | PATの権限不足。Contents と Actions の両方を Read and write にする |
| 「GitHubに書き込めませんでした（404）」 | `GITHUB_REPO` の書き方が違う（`ユーザー名/リポジトリ名`）。またはPATの対象リポジトリが違う |
| 検証で「失敗」になる | URLの末尾が `/api/webhook` か確認。Vercelのデプロイが完了しているかも確認 |
| 誕生日の朝に来ない | GitHubの「Actions」タブを見る。赤い印があればログに原因が日本語で出ています |
| ある日から登録できなくなった | PATの期限切れ。STEP 1-3 で作り直して、Vercel の `GITHUB_TOKEN` を入れ替える |

## あとから変えたくなったら

| 変えたいこと | どこ |
|---|---|
| 通知の時刻 | `.github/workflows/birthday.yml` の `cron`（UTC。日本時間 −9時間） |
| 何日前にも知らせるか | GitHub の Variables `NOTIFY_DAYS_BEFORE`（`0,3` など） |
| 通知の文面 | `scripts/customers.py` の `build_message` |
| LINEのコマンド | `webhook/api/webhook.js` の `handleText` |
| 台帳を直接直したい | GitHubで `data/customers.csv` を開いて編集（またはClaude Codeに頼む） |
