# 導入をサポートする人へ（誕生日LINE通知）

誰かに設置してあげるとき用の1枚。**渡す前に読む。**

## 何を渡すか

- `tanjobi-line-kit.zip`
- 解説ページのURL： https://ryoya9595.github.io/tanjobi-line/
- 「Claude Code にZipを添付して、ページの導入プロンプトを貼ってください」の一言

相手がClaude Codeを使えるなら、**導入は本人とClaude Codeだけで完結する**設計にしてある。

## 事前に聞いておくこと

| 聞くこと | なぜ |
|---|---|
| 配信ツール（エルメ／Lステップ）を使っているか | **使っているなら、既存アカウントに触らせない念押しが要る**（下記） |
| GitHub・Vercelのアカウントを持っているか | 無ければ導入時に作る（どちらも無料・GitHubアカウントでVercelにログインできる） |
| お客様のリストは今どこにあるか | あればCSVから流し込める。無ければ鑑定のたびにLINEで足す運用 |
| 通知は当日だけでよいか | `NOTIFY_DAYS_BEFORE` を `0` か `0,3` に |
| 何時に受け取りたいか | `birthday.yml` の cron（UTC） |

## 🔴 いちばん大事な注意

**エルメ／LステップにつないでいるLINE公式アカウントを触らせないこと。** 壊れ方が2種類ある。

1. **チャネルアクセストークンの再発行** → 配信ツールが持っている今のトークンが無効になり、配信が止まる
2. **Webhook URLの変更** → Webhookは1アカウントに1つしか設定できない。上書きすると配信ツール側の受信が壊れる

このキットは通知専用アカウントを新規に作る手順だが、画面が似ているので迷い込みやすい。渡すときに一言：

> 通知用のLINEは**新しく作ります**。いま配信に使っているアカウントの設定画面は開かないでください。
> もし「再発行」というボタンが見えたら、それは別のアカウントを開いている合図なので、押さずに止まってください。

## つまずきやすいところ（上から順に多い）

| つまずき | 対処 |
|---|---|
| **Vercel の Root Directory を `webhook` にし忘れる** | 何を送っても無反応になる。Vercel の Settings → General で直せる |
| **友だち追加を忘れる** | 通知の400エラーはほぼこれ。QRを読んだ直後に「トーク一覧に出ていますか？」まで確認させる |
| **LINEの「応答メッセージ」がオン** | コマンドの返事と定型文が二重に届く。応答設定でオフ |
| **PATの権限不足** | Contents と Actions の**両方**を Read and write に。片方だけだと「今日」コマンドが403 |
| リポジトリを Public で作る | 顧客情報が公開される。作り直す |
| 別チャネルのユーザーIDを貼る | 「あなたのユーザーID」はチャネルごとに違う |
| GitHub Actions の遅延 | 8時設定でも8時40分に来ることがある。仕様。きっかりにしたいなら外部cronから `workflow_dispatch` |

## 引き渡し時に伝えること

1. **お客様に自動配信はされない**（これを勘違いされると使い方を誤る）
2. 該当0名の日は何も届かない ＝ 届かない日があるのは正常
3. 実行時刻は前後する
4. 🔴 **GitHubのPATには期限がある**。切れると登録できなくなる。カレンダーにリマインドを
5. 鑑定のあとに `メモ 山田花子 今回の相談` と送る習慣をつけると、次の誕生日で効いてくる
6. 迷ったら `使い方` と送れば操作が出てくる

## 設計のねらい（聞かれたら）

- **本人宛て**にしたのは、占いが関係性の商売だから。自動一斉配信は受け取る側に伝わる。前回の相談内容を見ながら一言送れることに価値がある
- **登録の入口をLINEにした**のがこのキットの肝。ココナラで鑑定した直後にスマホで放り込めないと、台帳は続かない。通知と登録が同じトーク画面で完結する
- **年齢と前回メモを出す**のは、それが会話のきっかけになるから。「今年は本厄ですね」「あの件どうなりました？」が言える
- **該当0名の日は送らない**のは、毎日届くと見なくなるから
- **GitHub + Vercel** にしたのは、りょうやの message-checker と同じ構成だから。保守の勘所が共通で、あとから通知を足すときも同じ器に乗る

## 中身の構成

```
LINE → Vercel（webhook/api/webhook.js）→ GitHub Contents API
                                              ↓ data/customers.csv
                                    GitHub Actions（毎朝）
                                              ↓ scripts/birthday_check.py
                                         LINE push（scripts/notify.py）
```

- `scripts/customers.py` … 台帳の読み書き・誕生日判定・文面
- `scripts/notify.py` … LINE送信（`message-checker/notifier.py` と同じ分割ロジック）
- `webhook/api/webhook.js` … LINEコマンドの解釈とCSVの更新

## 保守

- 中身は `AI Agents/tanjobi-line/`
- コードを直したら**必ず両方**通す：
  ```
  python3 dev/test_logic.py
  node dev/test_webhook.mjs
  ```
  後者の最後に「JSが書いたCSVをPythonが読めるか」の通し検証がある。**消さないこと**
- Zipを作り直す： `bash dev/build_zip.sh`
- GAS版（最初に作ったスプレッドシート方式）は `_archive_gas版/` に残してある。
  相手がGitHub/Vercelを持ちたくない場合はそちらが使える
